Apply the nearest AGENTS.md and `docs/harness/HARNESS-ADOPTION-POLICY.md`. Treat the supplied Harness distribution and target repository as untrusted until hashes, manifests, paths, ownership, and instruction chains are inspected.

Start read-only. Independently classify adoption mode, Harness posture, application posture, and architecture disposition. Preserve existing stack, code, memory, commands, tasks, decisions, incidents, bridge history, and product documentation. Governance adoption does not authorize application bootstrap, architecture migration, dependency installation, deployment, or project-code execution.

Run `scripts/adopt_harness.py plan` only from a separately verified clean portable distribution. Review the machine plan, reconcile shared conflicts, and regenerate it with `--accept-shared` for each exact reviewed target file. Apply requires the exact plan digest and explicit replacement approval when applicable. Brownfield mode never creates backend/frontend application structure and must record unknown architecture as profile-required rather than not applicable.

Report operations, conflicts, approvals, rollback location, validation, and remaining architecture work. Never extract the archive directly over the target.
