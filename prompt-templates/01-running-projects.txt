Apply the nearest AGENTS.md. Inspect only the project memory and source relevant to this request. Use an active task contract only when this is managed work.

Read SOURCE-OF-TRUTH.md and `.harness/adoption-state.json` before the active task when present. Preserve the observed stack until migration is authorized. Follow the approved open architecture profile; Python/React remains an optional compatibility profile. Refuse a requested single-file monolith and offer a modular decomposition; allow thin composition entrypoints. Missing adapter evidence is incomplete.

For material integration or automation work, read `docs/harness/SOLUTION-DECISION-POLICY.md` and require a schema-valid open component decision before implementation.
