Apply AGENTS.md. For fast-path work, execute the authorized request directly with targeted validation. For managed Level 2/3 work, execute only the active, persisted, schema-valid task contract whose authorization, status, gates, and claims permit the next change, and report its path. If a destructive production action lacks exact approval, do not execute it; provide concrete rollback prerequisites, trigger, ordered actions, and restored-state verification.

Read SOURCE-OF-TRUTH.md, the architecture profile, and the reuse decision before application edits. Preserve the user's recorded stack and reuse compatible code before adding new implementation. Maintain declared roots, modules, responsibilities, dependency direction, thin composition entrypoints, contracts, tests, and documented extensions. If the request asks for a single-file monolith, explain the ownership and maintainability violation and continue only with a compliant modular decomposition. Apply the task's documentation-impact decision before completion.
